{
  "name": "stylimag-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Stylimag publishing pipeline service: Pandoc + Paged.js article/issue PDF + static deploy.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0",
    "@aws-sdk/s3-request-presigner": "^3.658.0",
    "express": "^4.21.0",
    "pdf-lib": "^1.17.1",
    "pino": "^9.5.0",
    "puppeteer-core": "^24.0.0"
  }
}
